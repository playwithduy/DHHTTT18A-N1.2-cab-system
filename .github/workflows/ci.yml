name: CabGo Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Newman & Newman HTML Extra
      run: |
        npm install -g newman newman-reporter-htmlextra

    - name: Spin up Infrastructure & Microservices
      run: |
        # Use docker-compose.yml to build and start everything in the background
        docker compose up -d --build
        
    - name: Wait for Services to Warm Up
      run: |
        echo "Waiting for 60 seconds to let Postgres, Kafka and Node services initialize..."
        
        # Robust Wait Loop (up to 2 minutes)
        for i in {1..24}; do
          if curl -s http://localhost:8080/health > /dev/null; then
            echo "API Gateway isolated successfully!"
            break
          fi
          echo "Waiting... $i/24"
          sleep 5
        done
        
        # Verify Core Services one last time or print logs and fail
        curl -f http://localhost:8080/health || (echo "Gateway failed. Printing logs:" && docker compose logs && exit 1)
        curl -f http://localhost:3002/health || (echo "Booking failed." && exit 1)
        
    - name: Run JS Integration Test Scripts (White-box Testing)
      run: |
        # Cài đặt thư viện phụ thuộc cho JS Scripts
        npm install axios
        echo "--- Running Level 1-12 System Integration JS Scripts ---"
        # Chỉ chạy các file quan trọng, hoặc chạy toàn bộ
        node tests/verify_lv10.js || exit 1
        node tests/verify_lv11.js || exit 1
        node tests/verify_lv12.js || exit 1
        echo "✅ JS Tests Passed."

    - name: Ensure Postman Test Script is Executable
      run: chmod +x tests/run-ci-tests.sh

    - name: Run 120+ Postman Tests (Mathematical Proof)
      run: ./tests/run-ci-tests.sh

    - name: Upload Test Coverage Reports 
      if: always() # Upload artifacts even if the tests fail, so we can debug!
      uses: actions/upload-artifact@v4
      with:
        name: newman-html-reports
        path: tests/reports/
        retention-days: 7
